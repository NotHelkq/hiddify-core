diff --git a/experimental/libbox/service.go b/experimental/libbox/service.go
index 0aaa51a5..ac74f536 100644
--- a/experimental/libbox/service.go
+++ b/experimental/libbox/service.go
@@ -132,7 +132,12 @@ func (w *platformInterfaceWrapper) NetworkInterfaces() ([]adapter.NetworkInterfa
 				Index:     int(netInterface.Index),
 				MTU:       int(netInterface.MTU),
 				Name:      netInterface.Name,
-				Addresses: common.Map(iteratorToArray[string](netInterface.Addresses), netip.MustParsePrefix),
+				Addresses: common.FlatMap(iteratorToArray[string](netInterface.Addresses), func(it string) []netip.Prefix {
+					if prefix, err := netip.ParsePrefix(it); err == nil {
+						return []netip.Prefix{prefix}
+					}
+					return nil
+				}),
 				Flags:     linkFlags(uint32(netInterface.Flags)),
 			},
 			Type:        C.InterfaceType(netInterface.Type),
@@ -212,7 +217,7 @@ func (w *platformInterfaceWrapper) FindConnectionOwner(request *adapter.FindConn
 	}
 
 	result, err := w.iif.FindConnectionOwner(request.IpProtocol, request.SourceAddress, request.SourcePort, request.DestinationAddress, request.DestinationPort)
-	if err != nil {
+	if err != nil || result == nil {
 		return nil, err
 	}
 	return &adapter.ConnectionOwner{
diff --git a/option/v2ray_transport.go b/option/v2ray_transport.go
index 3e2f3c2e..42d25519 100644
--- a/option/v2ray_transport.go
+++ b/option/v2ray_transport.go
@@ -199,7 +199,12 @@ func checkV2RayXHTTPBaseOptions(mode string, options *V2RayXHTTPBaseOptions) err
 		}
 	}
 	if options.XPaddingBytes.From <= 0 || options.XPaddingBytes.To <= 0 {
-		return E.New("xPaddingBytes cannot be disabled")
+		options.XPaddingBytes = Xbadoption.Range{
+			From: 100,
+			To:   1000,
+		}
+	} else if options.XPaddingBytes.From > options.XPaddingBytes.To {
+		options.XPaddingBytes.From, options.XPaddingBytes.To = options.XPaddingBytes.To, options.XPaddingBytes.From
 	}
 	if options.XPaddingKey == "" {
 		options.XPaddingKey = "x_padding"
diff --git a/protocol/hysteria2/outbound.go b/protocol/hysteria2/outbound.go
index 7651a0c6..47441d48 100644
--- a/protocol/hysteria2/outbound.go
+++ b/protocol/hysteria2/outbound.go
@@ -62,18 +62,19 @@ func NewOutbound(ctx context.Context, router adapter.Router, logger log.ContextL
 	var geckoPassword string
 	var geckoMinPacketSize, geckoMaxPacketSize int
 	if options.Obfs != nil {
-		if options.Obfs.Password == "" {
-			return nil, E.New("missing obfs password")
-		}
-		switch options.Obfs.Type {
-		case hysteria2.ObfsTypeSalamander:
-			salamanderPassword = options.Obfs.Password
-		case hysteria2.ObfsTypeGecko:
-			geckoPassword = options.Obfs.Password
-			geckoMinPacketSize = options.Obfs.GeckoOptions.MinPacketSize
-			geckoMaxPacketSize = options.Obfs.GeckoOptions.MaxPacketSize
-		default:
-			return nil, E.New("unknown obfs type: ", options.Obfs.Type)
+		if options.Obfs.Password == "" || options.Obfs.Type == "" {
+			options.Obfs = nil
+		} else {
+			switch options.Obfs.Type {
+			case hysteria2.ObfsTypeSalamander:
+				salamanderPassword = options.Obfs.Password
+			case hysteria2.ObfsTypeGecko:
+				geckoPassword = options.Obfs.Password
+				geckoMinPacketSize = options.Obfs.GeckoOptions.MinPacketSize
+				geckoMaxPacketSize = options.Obfs.GeckoOptions.MaxPacketSize
+			default:
+				options.Obfs = nil
+			}
 		}
 	}
 	outboundDialer, err := dialer.NewWithOptions(dialer.Options{
diff --git a/route/rule/rule_default.go b/route/rule/rule_default.go
index 02409c33..5b51a81a 100644
--- a/route/rule/rule_default.go
+++ b/route/rule/rule_default.go
@@ -126,13 +126,13 @@ func NewDefaultRule(ctx context.Context, logger log.ContextLogger, options optio
 		rule.allItems = append(rule.allItems, item)
 	}
 	if len(options.Geosite) > 0 {
-		return nil, E.New("geosite database is deprecated in sing-box 1.8.0 and removed in sing-box 1.12.0")
+		// deprecated: ignore rather than failing
 	}
 	if len(options.SourceGeoIP) > 0 {
-		return nil, E.New("geoip database is deprecated in sing-box 1.8.0 and removed in sing-box 1.12.0")
+		// deprecated: ignore rather than failing
 	}
 	if len(options.GeoIP) > 0 {
-		return nil, E.New("geoip database is deprecated in sing-box 1.8.0 and removed in sing-box 1.12.0")
+		// deprecated: ignore rather than failing
 	}
 	if len(options.SourceIPCIDR) > 0 {
 		item, err := NewIPCIDRItem(true, options.SourceIPCIDR)
diff --git a/route/rule/rule_dns.go b/route/rule/rule_dns.go
index 438c6fbd..68522742 100644
--- a/route/rule/rule_dns.go
+++ b/route/rule/rule_dns.go
@@ -143,13 +143,13 @@ func NewDefaultDNSRule(ctx context.Context, logger log.ContextLogger, options op
 		rule.allItems = append(rule.allItems, item)
 	}
 	if len(options.Geosite) > 0 { //nolint:staticcheck
-		return nil, E.New("geosite database is deprecated in sing-box 1.8.0 and removed in sing-box 1.12.0")
+		// deprecated: ignore rather than failing
 	}
 	if len(options.SourceGeoIP) > 0 {
-		return nil, E.New("geoip database is deprecated in sing-box 1.8.0 and removed in sing-box 1.12.0")
+		// deprecated: ignore rather than failing
 	}
 	if len(options.GeoIP) > 0 {
-		return nil, E.New("geoip database is deprecated in sing-box 1.8.0 and removed in sing-box 1.12.0")
+		// deprecated: ignore rather than failing
 	}
 	if len(options.SourceIPCIDR) > 0 {
 		item, err := NewIPCIDRItem(true, options.SourceIPCIDR)
diff --git a/route/rule/rule_item_rule_set.go b/route/rule/rule_item_rule_set.go
index 01364943..041d43df 100644
--- a/route/rule/rule_item_rule_set.go
+++ b/route/rule/rule_item_rule_set.go
@@ -5,7 +5,6 @@ import (
 
 	"github.com/sagernet/sing-box/adapter"
 	"github.com/sagernet/sing/common"
-	E "github.com/sagernet/sing/common/exceptions"
 	F "github.com/sagernet/sing/common/format"
 )
 
@@ -33,8 +32,7 @@ func (r *RuleSetItem) Start() error {
 	for _, tag := range r.tagList {
 		ruleSet, loaded := r.router.RuleSet(tag)
 		if !loaded {
-			_ = r.Close()
-			return E.New("rule-set not found: ", tag)
+			continue
 		}
 		ruleSet.IncRef()
 		r.setList = append(r.setList, ruleSet)
